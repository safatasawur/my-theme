{% comment %}
  This is a simple section with a schema.
{% endcomment %}

<div>
  <h2>{{ section.settings.heading }}</h2>
</div>

{% schema %}
{
  "name": "promotion group",
  "tag" : "section" ,
   "class" : "my-wrapper-class",
    "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading Text",
      "default": "Welcome!"
    }
  ],
  "presets": [
    {
      "name": "promotion group"
    }
  ]
}
{% endschema %}
